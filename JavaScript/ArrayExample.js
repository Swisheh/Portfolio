const weaponArray = ["sword", "axe", "hammer", "spear", "staff"];

weaponArray.push("dagger");

weaponArray.forEach(weapon => {
    console.log(weaponArray[weapon]); // display all weapon types
});